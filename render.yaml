services:
  - type: worker
    name: video-watermark-cleaner
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python bot.py"
    envVars:
      - key: BOT_TOKEN
        value: "твой_токен_бота"
